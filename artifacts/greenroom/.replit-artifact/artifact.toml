kind = "web"
previewPath = "/"
title = "Greenroom"
version = "1.0.0"
id = "artifacts/greenroom"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25356

[services.development]
run = "pnpm --filter @workspace/greenroom run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/greenroom", "run", "build" ]
publicDir = "artifacts/greenroom/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25356"
BASE_PATH = "/"
